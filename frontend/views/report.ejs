<div class="max-w-4xl mx-auto">
  <div class="mb-8 flex justify-between items-center">
    <h2 class="text-2xl font-bold" data-i18n="Test Report">Test Report</h2>
    <div class="space-x-3">
      <button onclick="window.print()" class="px-4 py-2 rounded-lg font-medium bg-slate-200 hover:bg-slate-300 transition-all text-sm">
        <span data-i18n="Print">🖨️ Print</span>
      </button>
      <a href="/" class="px-4 py-2 rounded-lg font-medium bg-slate-200 hover:bg-slate-300 transition-all text-sm">
        <span data-i18n="Dashboard">🏠 Dashboard</span>
      </a>
      <a href="/run/1" class="px-4 py-2 rounded-lg font-medium bg-emerald-500 hover:bg-emerald-600 transition-all text-white text-sm">
        <span data-i18n="Rerun Test">🔄 Rerun Test</span>
      </a>
    </div>
  </div>

  <!-- Summary Card -->
  <div class="bg-white p-6 rounded-xl shadow-sm border border-slate-100 mb-6">
    <h3 class="font-bold text-lg mb-4" data-i18n="Summary">📋 Summary</h3>
    <div class="grid grid-cols-2 gap-4 text-sm">
      <div><strong data-i18n="Scenario:">Scenario:</strong> <%= report.scenarioName %></div>
      <div><strong data-i18n="Run ID:">Run ID:</strong> <code><%= report.runId %></code></div>
      <div><strong data-i18n="Total Requests:">Total Requests:</strong> <%= report.totalRequests.toLocaleString() %></div>
      <div><strong data-i18n="Avg Latency:">Avg Latency:</strong> <%= report.avgLatency %>ms</div>
      <div><strong data-i18n="Error Rate:">Error Rate:</strong> <%= report.errorRate %>%</div>
      <div><strong data-i18n="Peak CPU:">Peak CPU:</strong> <%= report.peakCPU %>%</div>
    </div>
  </div>

  <!-- Recommendations -->
  <div class="bg-white p-6 rounded-xl shadow-sm border border-slate-100 mb-6">
    <h3 class="font-bold text-lg mb-4" data-i18n="Recommendations">💡 Recommendations</h3>
    <ul class="list-disc list-inside space-y-2">
      <% report.recommendations.forEach(rec => { %>
        <li><%= rec %></li>
      <% }) %>
    </ul>
  </div>

  <!-- Result Badge -->
  <div class="text-center py-6">
    <div class="inline-block px-6 py-3 bg-emerald-50 text-emerald-800 rounded-xl font-medium" data-i18n="Test Completed Successfully">
      ✅ Test Completed Successfully
    </div>
  </div>
</div>